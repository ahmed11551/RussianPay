name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  release:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
        
    - name: Get version
      id: version
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
        else
          echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        fi
        
    - name: Update version in Info.plist
      run: |
        VERSION="${{ steps.version.outputs.VERSION }}"
        VERSION_NUMBER="${VERSION#v}"
        
        # Update CFBundleShortVersionString
        /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VERSION_NUMBER" RussianPay/Info.plist
        
        # Update CFBundleVersion (build number)
        BUILD_NUMBER=$(date +%Y%m%d%H%M)
        /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" RussianPay/Info.plist
        
    - name: Build for release
      run: |
        xcodebuild -project RussianPay.xcodeproj \
                   -scheme RussianPay \
                   -destination 'generic/platform=iOS' \
                   -configuration Release \
                   clean build
                   
    - name: Archive app
      run: |
        xcodebuild -project RussianPay.xcodeproj \
                   -scheme RussianPay \
                   -destination 'generic/platform=iOS' \
                   -configuration Release \
                   archive -archivePath RussianPay.xcarchive
                   
    - name: Create release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.version.outputs.VERSION }}
        name: RussianPay ${{ steps.version.outputs.VERSION }}
        body: |
          ## 🚀 RussianPay ${{ steps.version.outputs.VERSION }}
          
          ### ✨ Новые возможности
          - Полный аналог Apple Pay для России
          - NFC эмулятор с поддержкой ISO 14443-A/B, FeliCa, ISO 15693
          - Интеграция с Apple Pay API
          - Альтернативные протоколы связи (Bluetooth, Wi-Fi Direct, QR-коды)
          - Высокий уровень безопасности (AES-256-GCM, Curve25519)
          
          ### 📱 Поддерживаемые устройства
          - iPhone (iOS 14.0+)
          - iPad (iPadOS 14.0+)
          
          ### 🔧 Установка
          1. Скачайте `.ipa` файл
          2. Установите через Xcode или AltStore
          3. Настройте сертификаты разработчика
          
          ### 📄 Документация
          - [README](README.md)
          - [Инструкции по сборке](BUILD_INSTRUCTIONS.md)
          - [Настройка GitHub](GITHUB_SETUP.md)
          
          ---
          **RussianPay** - делаем платежи доступными для всех! 🇷🇺
        files: |
          RussianPay.xcarchive
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
